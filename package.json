{
  "name": "nodejs-ci-cd-app",
  "version": "1.0.0",
  "description": "CI/CD Node.js App without Docker",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "jest",
    "build": "echo Build step (no compilation needed)"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.6.0",
    "supertest": "^6.3.3"
  }
}
